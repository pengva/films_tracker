<%= form_for(movie.comments.new, html: {'data-turbo' => "false"}) do |form| %>
  <%= form.text_area :body, class: "form-control " %>
  <%= form.hidden_field :movie_id, value: movie.id %>
  <%= form.submit class: 'btn btn-outline-success mt-3' %>
<% end %>
