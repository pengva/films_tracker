<div class="container text-center">
  <div class="row">
    <div class="col text-start">

      <% if @movie != nil %>
        <h2>Edit movie</h2>
        <%= render "form", movie: @movie, path: movie_path(@movie) %>
        <%= link_to "Back", @movie %>
    <% else %>
        <h2> Movie doesn't exist</h2>
        <%= link_to "Back", root_path %>
    <% end %>



    </div>
  </div>
</div>
